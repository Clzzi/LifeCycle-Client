import type { NextPage } from 'next';
import DateUtil from 'src/core/utils/date';

const Home: NextPage = () => {
  const dateUtil = new DateUtil();

  return (
    <div>
      <span>asdf</span>
    </div>
  );
};

export default Home;
